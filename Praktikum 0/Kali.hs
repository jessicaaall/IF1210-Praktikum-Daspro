module Kali where

    -- KALI                         kali(a,b)
    -- DEFINISI DAN SPESIFIKASI
    kali :: Int -> Int -> Int
    {- kali(a,b) menghasilkan nilai perkalian antara dua bilangan bulat a dan b. -}
    -- REALISASI
    kali a b = a*b

    
